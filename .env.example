# OpenAI Configuration
API_KEY=your_openai_api_key_here

# Optional: Specify the OpenAI model to use (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini

# Enhanced Backend Configuration
# URL for the enhanced Python backend with psychological profiling and conversation engine
VITE_API_BASE_URL=http://localhost:8000

# Instructions:
# 1. Copy this file to .env
# 2. Replace 'your_openai_api_key_here' with your actual OpenAI API key
# 3. Obtain an API key from: https://platform.openai.com/api-keys
# 4. Update VITE_API_BASE_URL if your enhanced backend runs on a different port/host
# 5. Keep your .env file private and never commit it to version control